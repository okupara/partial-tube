{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "storybook": "lerna exec --scope @partial-tube/frontend -- npm run storybook",
    "build:backend:dev": "",
    "build:backend:pro": "",
    "dev:frontend": "lerna exec --scope @partial-tube/frontend -- npm run dev",
    "dev:backend": "MODE=develop firebase serve",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "lerna": "^3.15.0",
    "prettier": "^1.18.2"
  }
}
